ls .               # Show contents of the current directory
cd <dir>           # Change Directory, meaning change
mkdir <dirname>    # Create new directory
touch <filename>   # Create new file
cd ..              # Go to one directory upwards in the tree.
rm <file>          # Removes/deletes the given file
rmdir <dirname>    # Removes an empty directory
rm -r <dirname>	   # Removes a non-empty directory
echo <message>     # Prints message
cat  <filename>    # Prints contents of files

sudo apt  install <packagename>  #installs package
which <command>   # prints location of the command
#ctrl + c = abbrechen
#ctrl + d = exit 


