git init #startbefehl für git
git status #in welchem Zustand der Ordner ist
git add <Filename> #staged (Bühne) das File
git commit #speichert Files auf stage
git diff <Filename> #shows differences
git log #whole history of the repository
repository #Ort an welchem alles gespeichert wird
git checkout <branch|commit> # anschauen/ go to )
branch # ast
git branch # shows all branches
git checkout exercices # Hausaufgaben
git remote add <remote> <url> #adds a remote repository
git push -u <remote> <branch>
